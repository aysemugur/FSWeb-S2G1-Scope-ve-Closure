last skills = this.skills.at(-1)
let aconstant =object.keys(user)
console.log(aconstant); 

let ajdskfd=object.values(user);
console.log(user)
console.log(object.entries(users));


for ( let [key, val] of Object.entries(user)){
  console.log(key, val)
}
  for (let [key,val] of Object.entries(user)){
    console.log(`${key} and ${val}`)
  }
const obj ={ prop:42}
  Object.freeze(obj);

  console.log(hasOwnProperty('country'))
}
function sayHello(){
  console.log(hello);

}
setInterval(sayHello,)

setInterval(sayhello,2000);
setTimeout(() =>{
  clearInterval(inteval);
  console.log('zamanlama islemi iptal edildi')
},5000)

Numbers.reduce((acc,first) => {
    return acc + first
},0)
console.log(
  users.every(user => user.gender==1 )
)\\

console.log(
  users.every(user => user.gender ==1)
)
names.sort((a,b) => b.localCompare(a))
console.log(
  users.find(user => user>3)
)
let caniko (() =>{
 if (caniko.id == 2){
      caniko.name ='hakan'
 }
},3000)

function takimSkoru(abc){
  let sonuc= Math.floor(Math.random()*15+10);
  return sonuc;
}
takimSkoru()

function macSonucu(takimSkoru,ceyrekSayisi){
  /*Kodunuzu buraya yazınız*/
}